<div class="justify-content-center">
  <div class="card mb-3">
    <%= render 'form', question: @question %>
    <div class="card-footer">
      <%= link_to t('back'), admin_quiz_path(@question.quiz), class: 'btn btn-primary' %>
    </div>
  </div>
</div>

