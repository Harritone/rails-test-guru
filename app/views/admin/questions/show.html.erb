<h1><%= @question.quiz.title %></h1>
<p>
  <%= @question.body %>
</p>

<%= link_to t('add'), new_admin_question_answer_path(@question) %>

<h2>Answers:</h2>

<ol>
  <%= render @question.answers %>
</ol>

<%= link_to t('edit'), edit_admin_question_path(@question) %> |
<%= link_to t('back'), admin_quiz_path(@question.quiz) %>
