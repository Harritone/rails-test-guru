<div class="justify-content-center">
  <div class="card mb-3">
    <div class="card-header">
      <h1><%= t('.header') %></h1>
    </div>
    <div class="card-body"><%= render 'form', quiz: @quiz %></div>
    <div class="card-footer">
      <%= link_to t('.back'), admin_quizzes_path, class: 'btn btn-primary' %>
    </div>
  </div>
</div>
